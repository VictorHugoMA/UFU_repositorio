#include<stdio.h>
#include<math.h>
    int n, m, i, j, soma=0, somaM=0;

    scanf("%d %d", &n, &m);

    int mat[n][m];

    for(i=0; i<n; i++){
        for(j=0; j<m; j++){
            scanf("%d", &mat[i][j]);
        }
    }

    for(i=0; i<m; i++){
        soma+=mat
    }