#include<stdio.h>

    int main(){
        int num, nivel, i;

        while(scanf("%d", &num) !=EOF){
            for(i=0; i<num; i++){
                scanf("%d", &nivel);
            }
        }

    }